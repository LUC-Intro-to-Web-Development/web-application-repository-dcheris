import { CrunchyrollAnime, CrunchyrollSeason, QueueEntry } from "../types";
export declare class Queue {
    static add: (animeResolvable: string | CrunchyrollAnime | CrunchyrollSeason) => Promise<QueueEntry>;
    static remove: (animeResolvable: string | CrunchyrollAnime | CrunchyrollSeason) => Promise<{
        data: boolean;
        error: boolean;
        code: string;
    }>;
    static show: () => Promise<QueueEntry[]>;
}
